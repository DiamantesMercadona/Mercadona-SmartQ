services:
  redis_mercadona:
    image: redis:latest
    container_name: redis_mercadona
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: