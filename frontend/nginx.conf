server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — redirige todo a index.html para que Vue Router funcione
    location / {
        try_files $uri $uri/ /index.html;
    }
}
